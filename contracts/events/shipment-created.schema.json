{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://event-hunter.local/schemas/shipment-created.schema.json",
  "title": "ShipmentCreated",
  "allOf": [
    { "$ref": "./canonical-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "ShipmentCreated" },
        "eventVersion": { "const": 1 },
        "aggregateType": { "const": "Shipment" },
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["shipmentId", "orderId", "provider", "status"],
          "properties": {
            "shipmentId": { "type": "string", "minLength": 1 },
            "orderId": { "type": "string", "minLength": 1 },
            "provider": { "type": "string", "minLength": 1 },
            "status": { "type": "string", "enum": ["CREATED"] }
          }
        }
      }
    }
  ]
}

