{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://event-hunter.local/schemas/order-created.schema.json",
  "title": "OrderCreated",
  "allOf": [
    { "$ref": "./canonical-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "OrderCreated" },
        "eventVersion": { "const": 1 },
        "aggregateType": { "const": "Order" },
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["orderId", "customerId", "totalAmount", "currency"],
          "properties": {
            "orderId": { "type": "string", "minLength": 1 },
            "customerId": { "type": "string", "minLength": 1 },
            "totalAmount": { "type": "number", "minimum": 0 },
            "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
            "simulationProfile": {
              "type": "string",
              "enum": ["NORMAL", "PAYMENT_FAILED", "SHIPMENT_DELIVERED", "RETURN_REFUND"]
            }
          }
        }
      }
    }
  ]
}
